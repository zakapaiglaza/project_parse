import { XPathContext } from '../xpath-types';
import { UnaryOperation } from './unary-operation';
export declare class UnaryMinusOperation extends UnaryOperation {
    evaluate(c: XPathContext): import("../xpath-types").XNumber;
    toString(): string;
}
