import { NamespaceResolver } from './xpath-types';
export declare class NodeXPathNSResolver {
    node?: Node;
    namespaceResolver: NamespaceResolver;
    constructor(n?: Node);
    lookupNamespaceURI(prefix: string): string | null;
}
